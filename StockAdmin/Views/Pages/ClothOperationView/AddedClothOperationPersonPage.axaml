<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:StockAdmin.Models"
             x:DataType="models:ClothOperationPerson"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="StockAdmin.Views.Pages.ClothOperationView.AddedClothOperationPersonPage">
    <StackPanel Width="300">
        <ComboBox 
            x:Name="CbClothOperationsPersons"
            DisplayMemberBinding="{Binding DataType=models:Person, Path=lastName}"
            SelectedValueBinding="{Binding DataType=models:Person, Path=id}"
            SelectedValue="{Binding Path=personId}"
            HorizontalAlignment="Stretch"
            />
        
        <CalendarDatePicker
            SelectedDate="{Binding dateStart}"/>
		
		<CheckBox x:Name="CbEnded"
                IsChecked="{Binding isEnded}"
                Content="Окончена ли?"/>
		
        <Button Content="Отправить" Click="SaveChanges"/>
    </StackPanel>
</UserControl>
